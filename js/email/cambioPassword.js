$(document).ready(function () {
    //-------------------------------------------------------------------------------
    VisibilityPassword("show-passwords", [
        "modal-newPassword_2",
        "modal-confirmPassword_2",
    ]);
    //-------------------------------------------------------------------------------
    // Función para encriptar una cadena en SHA-256
});